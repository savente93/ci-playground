"""No time for docstrings, maths to be done."""


def add_one(x):
    """Add one to the input."""
    return x + 1


def rases_error():
    """Raise an error."""
    raise RuntimeError(
        "Nice tests you got there. would be a shame if they would start failing."
    )
